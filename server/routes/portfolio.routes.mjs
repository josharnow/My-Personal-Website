import * as PortfolioController from '../controllers/portfolio.controller.mjs';

export default (app) => {
  // app.get('/api/login', PortfolioController.login);
  // app.get('*', PortfolioController.deployment);

}